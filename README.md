Convert this to windows powershell
mkdir -p /srv/formulas
cd /srv/formulas
git clone https://github.com/smarlof.....

# or

mkdir -p /srv/formulas
cd /srv/formulas
wget https://github.com/marlof..../archive/master.tar.gz
tar xf solr-formula-master.tar.gz
